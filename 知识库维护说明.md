# 知识库维护说明

## 本轮范围与依据（2026-09-05）

仓库：Wuyf9/KnowledgeBase，main。开始读取的版本为 ed17094f28403614315d415692410f041d71fef1。

本轮排除全部机器人控制、运动、调度与未落地控制架构。已有项目文件保留，未删除历史资料；本轮导航不纳入该主题，通用笔记不复制相关代码或业务命令。

已检查完整远程目录树及所有相关旧笔记。两篇实质技术笔记为 Git 安装、AI 集成；systemd、TCP 重连和 Redis NOGROUP 是已有空文件。按内容而非目录名称决定补充位置，保留这些旧路径。

读取前序筛选对话及当前可列出的相关历史；这不是账号全部历史全文检索。前序列表的“高频”是候选依据，不是统计次数。本轮复核了 Git 配置、Obsidian 对接、cron、Linux 日志、TCP、YARP、Redis、MySQL、海康相关记录；Redis 恢复日志和海康通道 XML 另行读取。缺少最终回执的内容不补造成功状态。

## 文件规划与分批提交

| 批次 | 补充旧文件 | 新增边界明确的内容 |
| --- | --- | --- |
| 1：Git 与集成 | Git 安装配置、ChatGPT/Codex/Obsidian/GitHub 集成 | 无；系统 Git、SSH、同步与能力边界均归入旧篇 |
| 2：Linux 与部署 | systemd 服务管理空文件 | cron 身份权限；部署方法；一个 unit 模板和一个只读诊断脚本 |
| 3：网络与后端 | TCP 重连空文件 | 网络/HTTP/代理统一入口；.NET 配置与生命周期 |
| 4：数据、设备与索引 | Redis NOGROUP 空文件、根 README、Problem 模板 | Redis 连接与类型；MySQL/EF Core；海康；案例索引；本维护说明 |

## 唯一主文档与关联

- Linux 服务命令只在 systemd 篇维护，数据库和部署笔记链接引用。
- Git 安装、系统 Git、SSH 和同步只在 Git 篇维护；AI 集成只解释链路、验收与会话能力。
- IP、端口、TLS、HTTP 和代理集中在网络篇，TCP 篇只保留长连接自身生命周期。
- .NET 篇维护通用作用域、取消与释放；EF Core 篇维护数据库专属生命周期。
- Redis 通用连接、类型和超时在技术篇，NOGROUP 在旧案例篇；不另建相同错误教程。
- 海康放在 03_Hardware/Hikvision，保留型号/固件边界，不与通用 HTTP 排查混写。
- 部署脚本正文只保存一份，说明文档直接链接；案例索引不再复制修复步骤。

## 去重和状态规则

每次写入先读取现有主文档与相邻主题，搜索关键词、同义词、异常名。已存在的知识只补缺失前提、差异、证据或关联；没有新增信息则跳过，不增加重复“总结版”。

证据分为：用户确认、原始日志验证、旧记录转述、通用参考、待验证方案。官方资料支持技术规则，不证明用户现场已执行成功。运行命令、修复输出、业务验收和观察时段齐全后才标记完整闭环；多阶段流程分别记录状态。

本轮明确修正：不以 chmod 777 为默认；不以线程池单次快照排除超时原因；不把健康检查当当前请求重试；不把 Redis 读到消息当外部推送成功；不把 MCP 根路径可访问当工具握手成功。

资料只保留脱敏特征及可追踪对话 ID，不把完整聊天、业务凭据或大附件复制进公开仓库。示例参数不代表实际生产配置。

## 检查与后续更新

提交前检查旧文件版本、路径、双链、重复段落、冲突标记和模板占位；更新分支使用非强制方式，远端改变时重新合并。提交后回读树和变更文件。脚本未在目标 Linux 执行、数据库和设备未现场测试，均须保留此限制。

后续取得成功回执时更新原文的状态和证据，不再新建一篇同主题成功总结。故障入口：[[04_Troubleshooting/README]]。
